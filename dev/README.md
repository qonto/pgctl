# Development Folder

This folder contains:
* a Docker Compose file you can use to spawn a local Postgres database
* an example config file for `pgctl`

To start your local Postgres instance, run:

```sh
make local-postgres
```
